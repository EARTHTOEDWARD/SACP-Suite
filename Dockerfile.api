FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml /app/pyproject.toml
RUN pip install -U pip && pip install -e ".[api]"
COPY src /app/src
ENV SACP_BIND=0.0.0.0 SACP_PORT=8000
EXPOSE 8000
CMD ["uvicorn", "sacp_suite.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
